# Agent instructions

See [`CLAUDE.md`](CLAUDE.md), which is the single source for this project's
goals, ground rules, and working protocol. It applies to any agent or
contributor, not only Claude Code — the filename is a convention, not a scope.

Kept as a pointer rather than a copy so the two cannot drift apart.
